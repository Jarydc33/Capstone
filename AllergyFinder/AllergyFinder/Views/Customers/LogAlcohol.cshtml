@model AllergyFinder.Models.LogAlcoholViewModel
@{
    ViewBag.Title = "LogAlcoholViewModel";
}

<h2>LogAlcoholViewModel</h2>

@using (Html.BeginForm())
{
<div class="form-horizontal">
    <h4>Alcohol Search</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="form-group">
        @Html.Label("Beer Name", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EditorFor(model => model.beerName, new { htmlAttributes = new { @class = "form-control" } })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Search" class="btn btn-default" />
        </div>
    </div>
</div>
}

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Food Name")
        </th>
        <th></th>

    </tr>
    @{
        if (Model != null && Model.retrievedFoods != null)
        {
            foreach (var item in Model.retrievedFoods)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.name)
                    </td>
                    <td>
                        @Html.ActionLink("Find Allergens", "FindFoodInfo", new { NDBNo = item.ndbno, route = 0 })
                    </td>
                    <td>
                        @Html.ActionLink("Log This Food", "FindFoodInfo", new { NDBNo = item.ndbno, route = 1 })
                    </td>

                </tr>
            }
        }
    }


</table>*@