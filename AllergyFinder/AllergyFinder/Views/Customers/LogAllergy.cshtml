@model AllergyFinder.Models.AddAllergenViewModel
@{
    ViewBag.Title = "LogAllergy";
}

<h2>LogAllergy</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Customer</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @*<div class="form-group">
            @Html.Label("Select an Allergen to Add", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.id,(SelectList)Model.allergens, new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>*@

        <div class="form-group">
            @Html.Label("Select an allergy to add", htmlAttributes: new { @class="control-label col-md-2"})
            @Html.DropDownListFor(model => model.id, (SelectList)Model.allergens, new { htmlAttributes = "control-label col-md-2" })
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}